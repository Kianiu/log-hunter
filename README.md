# 🕵️‍♂️ Log Hunter (Server Log Analyzer)

A lightweight and efficient Bash script designed to analyze server logs, identify user activities, and detect potential security threats (brute-force attacks).

## 🚀 Features
- Traffic Overview: Counts the total number of log entries.
- User Activity Analysis: Identifies active users and their login frequencies.
- Security Check: Detects suspicious IP addresses causing failed login attempts.

## 🛠️ Technologies Used
- OS: Linux (Ubuntu/Debian)
- Shell: Bash Scripting
- Commands: grep, cut, sort, uniq, wc, pipes (|)

## 💻 How to Run
1. Clone the repository:
   `bash
   git clone https://github.com/Kianiu/log-hunter.git
~~~

~~~
2. Navigate to the folder:
~~~ bash
cd log-hunter
~~~

3. Run the script:
~~~ bash
bash analyze.sh
~~~

---
Created by Kian
